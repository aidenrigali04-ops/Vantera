import { ComingSoon } from "@/components/coming-soon";

export default function AnalyticsPage() {
  return (
    <ComingSoon
      title="Analytics"
      description="Track sends, replies, meetings booked, and progress toward your revenue goal."
      unlocks="Unlocks once campaigns are running."
    />
  );
}
