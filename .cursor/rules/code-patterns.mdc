---
description: Required code patterns — server actions, API shape, forms, feature flags
alwaysApply: true
---

# Server Action

```ts
export async function actionName(input: z.infer<typeof inputSchema>) {
  const session = await requireAdminSession()
  const validated = inputSchema.parse(input)
  try {
    const result = await db.transaction(async (tx) => {
      // multi-table writes go here
    })
    return { success: true, data: result }
  } catch (error) {
    console.error('[actionName]', error)
    return { success: false, error: 'Operation failed' }
  }
}
```

# API Response Shape

Always return one of these two shapes — never anything else.

```ts
{ success: true, data: T }
{ success: false, error: string, code?: string }
```

# React Hook Form + Zod

```ts
const form = useForm<z.infer<typeof formSchema>>({
  resolver: zodResolver(formSchema),
  defaultValues: { ... },
})
```

# Feature Flag Check (server)

```ts
const isEnabled = await evaluateFlag({
  accountId: session.accountId,
  plan: session.plan,
  flagName: 'feature_name',
})
if (!isEnabled) return { success: false, error: 'Feature not available', code: 'FEATURE_GATED' }
```
