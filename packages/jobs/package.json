{
  "name": "@vantera/jobs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "type-check": "tsc --noEmit",
    "dev": "trigger dev",
    "deploy": "trigger deploy",
    "test": "vitest run"
  },
  "dependencies": {
    "@trigger.dev/sdk": "^4.0.0",
    "@supabase/supabase-js": "^2.108.1"
  },
  "devDependencies": {
    "trigger.dev": "^4.0.0",
    "typescript": "^5.8.0",
    "vitest": "^4.0.0"
  }
}
